export enum UserRole {
    ADMIN = 'admin',
    OWNER = 'owner',
}

export enum AppRoute {
    AUTH = 'auth',
    LOGIN = 'login',
    ADMIN = 'admin',
    OWNER = 'owner',
    RESTAURANTS = 'restaurants',
}
